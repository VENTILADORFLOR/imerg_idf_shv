# ── Google Earth Engine (Step 1: data extraction) ─────────────────────────────
earthengine-api>=0.1.370

# ── Core data processing (Steps 2–3: annual maxima, IDF fitting) ──────────────
pandas>=1.5.0
numpy>=1.23.0
scipy>=1.9.0
openpyxl>=3.1.0          # Excel export (.xlsx)

# ── Geospatial (Step 4: watershed delineation and figure) ─────────────────────
geopandas>=0.13.0
fiona>=1.9.0              # shapefile I/O backend for geopandas
shapely>=2.0.0
rasterio>=1.3.0
pysheds>=0.3.5            # DEM hydrological conditioning and watershed delineation

# ── Visualisation (Step 4: catchment figure) ──────────────────────────────────
matplotlib>=3.7.0
